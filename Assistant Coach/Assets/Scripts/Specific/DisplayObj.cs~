using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DisplayObj : MonoBehaviour {


	public void RemoveBlock ()
	{
		FindObjectOfType<CreationManager> ().RemoveBlock (gameObject);
		Destroy (gameObject);
	}

}
